# SMTP Email Configuration
# For Gmail: Use smtp.gmail.com with an App Password (not your regular password)
# For other providers: Check your email provider's SMTP settings

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password-here

# Email address where quote requests should be sent
# If not set, it will use SMTP_USER as the recipient
CONTACT_EMAIL=info@vegasvogueexpo.com
